<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.portal.mapper.BoardCommentMapper">

    <sql id="allColumns">
        COMMENT_ID, POST_ID, UPPER_COMMENT_ID, CONTENT, SECRET_YN,
        REG_USER_NM, REG_USER_ID, REG_IP, REG_DT,
        MOD_USER_ID, MOD_DT, DEL_YN, DEL_DT
    </sql>

    <!-- 게시글 댓글 목록 조회 -->
    <select id="selectByPostId" resultType="com.example.portal.entity.BoardComment">
        SELECT <include refid="allColumns"/>
        FROM TB_BBS_COMMENT
        WHERE POST_ID = #{postId}
          AND DEL_YN = 'N'
        ORDER BY COMMENT_ID ASC
    </select>

    <!-- 댓글 상세 조회 -->
    <select id="selectByCommentId" resultType="com.example.portal.entity.BoardComment">
        SELECT <include refid="allColumns"/>
        FROM TB_BBS_COMMENT
        WHERE COMMENT_ID = #{commentId}
    </select>

    <!-- 댓글 등록: selectKey로 시퀀스 발급 후 응답에 ID 포함 -->
    <insert id="insertComment" parameterType="com.example.portal.entity.BoardComment">
        <selectKey keyProperty="commentId" resultType="long" order="BEFORE">
            SELECT SEQ_TB_BBS_COMMENT.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TB_BBS_COMMENT (
            COMMENT_ID, POST_ID, UPPER_COMMENT_ID, CONTENT, SECRET_YN,
            REG_USER_NM, REG_USER_ID, REG_IP, REG_DT
        ) VALUES (
            #{commentId}, #{postId}, #{upperCommentId}, #{content}, #{secretYn},
            #{regUserNm}, #{regUserId}, #{regIp}, SYSDATE
        )
    </insert>

    <!-- 댓글 수정 -->
    <update id="updateComment" parameterType="com.example.portal.entity.BoardComment">
        UPDATE TB_BBS_COMMENT
        SET CONTENT = #{content},
            MOD_USER_ID = #{modUserId},
            MOD_DT = SYSDATE
        WHERE COMMENT_ID = #{commentId}
    </update>

    <!-- 댓글 Soft Delete -->
    <update id="deleteComment">
        UPDATE TB_BBS_COMMENT
        SET DEL_YN = 'Y',
            DEL_DT = SYSDATE
        WHERE COMMENT_ID = #{commentId}
    </update>

    <!-- 게시글 댓글 건수 -->
    <select id="countByPostId" resultType="int">
        SELECT COUNT(*)
        FROM TB_BBS_COMMENT
        WHERE POST_ID = #{postId}
          AND DEL_YN = 'N'
    </select>

</mapper>
