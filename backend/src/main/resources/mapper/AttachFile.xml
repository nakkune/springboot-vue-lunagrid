<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.portal.mapper.AttachFileMapper">

    <sql id="allColumns">
        ATCH_FILE_GRP_ID, FILE_SEQ, ORIGINAL_FILE_NM, STORED_FILE_NM,
        FILE_PATH, FILE_SIZE, FILE_EXT, MIME_TYPE, DOWN_CNT,
        REG_USER_ID, REG_DT, DEL_YN
    </sql>

    <!-- 그룹별 파일 목록 조회 -->
    <select id="selectByGrpId" resultType="com.example.portal.entity.AttachFile">
        SELECT <include refid="allColumns"/>
        FROM TB_COM_ATTACH_FILE
        WHERE ATCH_FILE_GRP_ID = #{atchFileGrpId}
          AND DEL_YN = 'N'
        ORDER BY FILE_SEQ ASC
    </select>

    <!-- 파일 등록 -->
    <insert id="insertFile" parameterType="com.example.portal.entity.AttachFile">
        INSERT INTO TB_COM_ATTACH_FILE (
            ATCH_FILE_GRP_ID, FILE_SEQ, ORIGINAL_FILE_NM, STORED_FILE_NM,
            FILE_PATH, FILE_SIZE, FILE_EXT, MIME_TYPE,
            REG_USER_ID, REG_DT, DEL_YN
        ) VALUES (
            #{atchFileGrpId}, #{fileSeq}, #{originalFileNm}, #{storedFileNm},
            #{filePath}, #{fileSize}, #{fileExt}, #{mimeType},
            #{regUserId}, SYSDATE, 'N'
        )
    </insert>

    <!-- 파일 Soft Delete -->
    <update id="deleteFile">
        UPDATE TB_COM_ATTACH_FILE
        SET DEL_YN = 'Y'
        WHERE ATCH_FILE_GRP_ID = #{atchFileGrpId}
          AND FILE_SEQ = #{fileSeq}
    </update>

    <!-- 그룹 전체 삭제 -->
    <update id="deleteByGrpId">
        UPDATE TB_COM_ATTACH_FILE
        SET DEL_YN = 'Y'
        WHERE ATCH_FILE_GRP_ID = #{atchFileGrpId}
    </update>

    <!-- 다운로드 횟수 증가 -->
    <update id="incrementDownCnt">
        UPDATE TB_COM_ATTACH_FILE
        SET DOWN_CNT = DOWN_CNT + 1
        WHERE ATCH_FILE_GRP_ID = #{atchFileGrpId}
          AND FILE_SEQ = #{fileSeq}
    </update>

</mapper>
